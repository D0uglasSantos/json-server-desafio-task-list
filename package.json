{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install",
    "prestart": "chmod +x node_modules/.bin/json-server",
    "start": "npx json-server --watch db.json --port 3001 --static ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}